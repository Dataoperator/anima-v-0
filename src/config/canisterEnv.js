import { CANISTER_IDS, NETWORK_CONFIG } from './index';

export const canisterEnv = {
  anima: CANISTER_IDS.anima,
  anima_assets: CANISTER_IDS.anima_assets,
  host: NETWORK_CONFIG.host,
  isLocal: NETWORK_CONFIG.isLocal,
  network: NETWORK_CONFIG.network
};